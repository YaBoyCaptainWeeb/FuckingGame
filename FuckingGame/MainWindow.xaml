<Window x:Class="FuckingGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FuckingGame"
        mc:Ignorable="d"
        Title="Fucking Game" x:Name="GameWindow" Height="700" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="900" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="ASSets/Mountain-Dusk.png"/>
        </Grid.Background>
        <Viewbox x:Name="BoxMap" Grid.Row="1" Grid.Column="1">
            <Canvas x:Name="CanvasMap" OpacityMask="Black" Loaded="Map_Loaded"  />
        </Viewbox>
        
        
        <StackPanel Grid.Row="1" Grid.Column="0"> 
            <Image x:Name="img1" Source="{Binding Path=Portrait}"/>
        </StackPanel>

    </Grid>
</Window>
